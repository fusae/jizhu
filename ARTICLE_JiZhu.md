# 微信聊天里的待办，总忘？写了个小工具帮我记着

你有没有这种经历——微信群或者私聊里有人说"周五下班前把方案发我""明天下午三点开会""下周记得续费"，你回了个"好的"，然后……就忘了。

手机备忘录太重，番茄钟太烦，待办 App 动不动就要注册账号、建项目、设优先级。我只是想从聊天记录里抓一句话，挂个截止时间，到点弹个通知，就这么简单的事。

所以做了「记住」。

## 它能干什么

一句话概括：**复制聊天内容 → 按快捷键 → AI 帮你填好 → 确认保存**。

打开快速添加窗口的时候，它会自动读取剪贴板里的文字。如果你配了 DeepSeek 的 API Key，AI 会自动从那堆聊天记录里抽出两件事：**要做什么**、**什么时候截止**。你确认一下，点保存，完事。

截止时间支持自然语言。写"明天下午 6 点""周五""下周三""10 分钟后"都行，不需要手动挑日期。窗口里还有几个快捷按钮——今天 18:00、明天 18:00、本周五、下周一——大部分场景点一下就够了。

## 提醒

截止时间到了不会悄悄过去。记住会在三个时间点弹系统通知：

- **提前 1 小时** — 还有时间处理
- **提前 15 分钟** — 该收尾了
- **到点** — 截止了

用的是操作系统原生的通知，macOS 右上角弹一下，Windows 右下角弹一下，不会错过。通知里直接显示任务内容，点一下打开主窗口。

另外有个细节：如果你创建任务的时候离截止时间已经很近了（比如设了 5 分钟后截止），它不会在 1 小时和 15 分钟的节点重复提醒，直接等到点通知你。

## 附件

快速添加窗口底部有个拖拽区域，可以直接把文件拖进去。比如有人微信发了个文档过来，你保存到本地，拖进记住挂到这条任务上。之后在任务列表里点附件就能直接打开，不用再翻聊天记录找文件。

## 主界面

主窗口很小，480×640，左边是任务列表。每条任务显示截止时间（带倒计时和颜色区分——快到了是橙色，马上到了是红色）、备注、原文内容。支持完成、恢复、编辑、删除。已完成和待办分 Tab 显示，不会混在一起。

macOS 上关掉窗口不会退出应用，它缩到菜单栏里待着。菜单栏图标旁边显示待办数量，一眼就知道还有几件事没做。点击图标打开主窗口，右键有菜单可以退出。

## 全局快捷键

`Cmd+Shift+T`（Mac）/ `Ctrl+Shift+T`（Windows），任何时候按一下，弹出快速添加窗口。无框、置顶、自动读剪贴板，填完 `Cmd+Enter` 提交，`Esc` 关掉。整个流程不需要切到记住的主窗口，手不用离开键盘。

## 跨平台

macOS 和 Windows 都支持。macOS 打 DMG 安装，Windows 用 NSIS 安装包（安装时可以选目录）。数据全部存在本地 SQLite 文件里，不需要联网，不需要账号。

## 自动更新

内置了 electron-updater，连接 GitHub Releases。设置面板里可以检查更新、下载、重启安装。打包发布新版本之后，用户不用自己跑去 GitHub 下，应用里一键搞定。

## AI 解析是可选的

不配 API Key 完全不影响使用，只是快速添加的时候备注和截止时间需要自己手填。配了之后，粘贴聊天内容会自动识别。用的是 DeepSeek 的接口，调一次大概 0.001 元人民币，一天用几十次也花不了几分钱。

## 技术栈

给可能想参考或二次开发的独立开发者说一句：Electron + TypeScript，数据库用的 better-sqlite3（本地文件，零配置），时间解析用的 chrono-node（支持中文自然语言），AI 解析调 DeepSeek API。代码结构很直——main 进程处理数据库、提醒、系统交互，renderer 就是原生 HTML/CSS/JS，没用框架。打包用的 electron-builder，配置都在 package.json 里。

整个项目依赖很少，代码也不复杂。如果你想做一个类似的桌面小工具，这个项目可以当个起点。

## 适合什么场景

想了想自己日常用它的场景：

- 微信群里接到的临时任务，随手粘进去
- 朋友约饭定时间，记下来免得忘
- 付款截止日期、快递取件码这种琐碎的事
- 工作中别人口头说的一句"那个东西这周给我"
- 任何"到时候提醒我一下"的时刻

说白了就是：**不严肃的待办、碎片化的提醒**。那些需要甘特图、需要拆子任务、需要协作的，不归它管。但日常那些"一句话的事"，用它记比什么都快。

## 最后

记住没有账号系统，没有云同步，没有订阅付费。数据在你自己电脑上，用不用 AI 都行。想试的话直接下安装包，或者 clone 下来 `npm install && npm start`。

开源地址：[github.com/fusae/jizhu](https://github.com/fusae/jizhu)
